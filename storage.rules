rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read & write access for uploaded media
    match /uploads/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
